package poo.gettersetter;

public class construtores {

    /*
     * Construtores
     * 
     * Sabemos que para criar um objeto na linguagem JAVA utilizamos a seguinte
     * estrutura de código.
     * 
     * Classe novoObjeto = new Classe();
     * 
     * Desta forma será criao um novoi objeto na memória, este recurso também é
     * conhecido
     * como instancia um novo objeto.
     * 
     * Muitas das vezes já queremos que na criação (instanciação) de um objeto, a
     * linguagem
     * já solicita para quem for criar este novo objeto defina algumas propriedades
     * essenciais. Abaixo iremos ilustrar uma classe Pessoa onde a mesma
     * terá os atributos: Nome, CPF, Endereço.
     * 
     * public Pessoa(String cpf, String nome) {
     * this.cpf = cpf;
     * this.nome = nome;
     * }
     * Pessoa marcos = new Pessoa("123", "Marcos");
     * 
     * Método This.
     * 
     */

}
